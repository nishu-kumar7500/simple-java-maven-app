pipeline
{
    agent {
        label 'jenkins-test'
    }

    tools {
        maven 'mymaven'
    }
    stages {
        stage('build')
    {
            steps {
                sh 'mvn clean package'
            }
            post {
                success {
                    archiveArtfacts artifacts: '**/target/*.war'
                }
            }
    }
    }
}
